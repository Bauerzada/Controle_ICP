function instalarOperacaoICP(){const ss=getOpsDb_();ensureOpsTabs_(ss);return {ok:true,id:ss.getId(),url:ss.getUrl()}}
function getOperacaoBootstrap_(){const fila=listarFilaPreparoICP_(),ativ=listarAtividadesICP_(),curvas=listarCurvasICP_(),cq=listarCQICP_(),seq=listarSequenciasICP_(),eq=listarEquipamentoICP_(),erros=listarErrosICP_();return {fila,atividades:ativ,curvas,cq,sequencias:seq,equipamento:eq,erros,resumo:{filaAberta:fila.filter(x=>!['Liberado','Concluído'].includes(String(x.Status))).length,atividadesAbertas:ativ.filter(x=>String(x.Status).toLowerCase()!=='resolvida').length,cqFora:cq.filter(x=>x.Status==='Fora do limite').length,sequenciasAbertas:seq.filter(x=>!['Concluída','Finalizada'].includes(String(x.Status))).length,ocorrenciasAbertas:eq.filter(x=>String(x.Status).toLowerCase()!=='concluído').length}}}
